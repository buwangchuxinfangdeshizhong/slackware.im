{% extends "SlackissSlackwareBundle:layout:layout.html.twig" %}

{% block title %}
编辑常见问题
{% endblock %}

{% block body -%}
    <h2>编辑: {{entity.title }}</h2>

    <form action="{{ path('faq_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" />
        {{ form_widget(edit_form) }}
        <p>
            <button type="submit">保存</button>
        </p>
    </form>

{% endblock %}
